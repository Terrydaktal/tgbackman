# Compatibility requirements for deployments that still install from a
# requirements file.  Development installs should prefer `uv sync` so the
# project metadata and lockfile remain the single source of truth.
beautifulsoup4>=4.12
ijson>=3.2,<4
Telethon>=1.36,<2
